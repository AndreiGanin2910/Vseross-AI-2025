# Task C — прогноз оценки заведения

**Результат:** leaderboard score `0.81`; OOF MAE `0.2336`.

## Решение

- статистические и географические признаки;
- агрегации по отзывам и объектам;
- TF-IDF с понижением размерности;
- LightGBM с out-of-fold валидацией;
- изотоническая калибровка применяется только если улучшает OOF MAE.

Положите `train.tsv`, `test.tsv` и `reviews.tsv` в `data/`. Запустите `solution.ipynb`; сабмит появится в `outputs/`.
